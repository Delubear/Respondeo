{
  // NOTE: This is a Blazor WebAssembly app, so this file is published as a static asset and is
  // publicly downloadable in the browser (e.g. GET /appsettings.json). It is safe for UI toggles
  // like the feature flags below, but must NEVER contain secrets, API keys, or connection strings.
  // Per-environment overrides go in wwwroot/appsettings.{Environment}.json and merge over this file.
  "FeatureFlags": {
    "AquinasPortrait": true,
    "SummaPillar": true,
    "MiraclesPillar": true
  }
}
